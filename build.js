module.exports = function (craftix, config) {
    // If you want to apply things like Sass compilation, do it here,
    // but don't forget to change buildFolder to something like 'build' in the config
    // (or wherever you put your build files), or let '.' if you do not build anything
};